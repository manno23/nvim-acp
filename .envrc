path_add ACP_WS_URL "ws://127.0.0.1:9876"    # default websocket endpoint (e.g. `wss://example.com/acp`).
path_add ACP_TOKEN "${OPENAI_API_KEY}"       # bearer token injected as `Authorization` header.B
path_add ACP_LOG_LEVEL "error"               # Lua-side log verbosity (`error`, `warn`, etc.).

